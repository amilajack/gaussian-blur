var _rafLoop = require("raf-loop");
var _parcelHelpers = require("@parcel/transformer-js/lib/esmodule-helpers.js");
var _rafLoopDefault = _parcelHelpers.interopDefault(_rafLoop);
var _src = require("../src");
var _srcDefault = _parcelHelpers.interopDefault(_src);
var _imgDemoJpg = require("../img/demo.jpg");
var _imgDemoJpgDefault = _parcelHelpers.interopDefault(_imgDemoJpg);
(async () => {
  const canvas = document.querySelector("canvas");
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  const img = new Image();
  img.src = _imgDemoJpgDefault.default;
  await new Promise(resolve => {
    img.onload = resolve;
  });
  const blur = new _srcDefault.default(canvas, img);
  blur.draw(2);
  const slider = document.querySelector("input");
  const output = document.getElementById("output");
  const blurRadius = 0;
  output.innerHTML = `Blur radius: ${blurRadius}`;
  let speed = 1;
  slider.oninput = () => {
    speed = parseInt(slider.value, 10);
  };
  let time = 0;
  function render(dt) {
    time += dt * speed / 1000;
    const anim = Math.sin(time) * 0.5 + 0.5;
    const iterations = 8;
    blur.draw(iterations, anim);
  }
  _rafLoopDefault.default(render).start();
})();
